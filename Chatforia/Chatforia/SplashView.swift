import SwiftUI

struct SplashView: View {
    var body: some View {
        VStack(spacing: 12) {
            ProgressView()
            Text("Loading Chatforia…")
        }
        .padding()
    }
}

